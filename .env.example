# アラート通知（Discord Webhook）
# チャンネル設定 → 連携サービス → Webhook で URL を取得
# 未設定ならアラートは無効（エラー・異常検出時も通知しない）
DISCORD_WEBHOOK_URL=

# BLS（米国労働統計局）API - 登録推奨（1日500リクエスト）。未登録は制限あり。
# https://data.bls.gov/registrationEngine/
# BLS_API_KEY=

# ローカル開発時: トレンドAPIをダミーデータで上書き（true のとき全トレンドがダミーを返す。APIキー不要）
# USE_DUMMY_DATA=true

# スケジューラ: 単一インスタンスで0本になる場合は false にするとファイルロックのみで確実に1本だけ実行
# TRENDS_SCHEDULER_USE_DB_LOCK=false

# トレンド一括取得の同時実行数（1:00/7:00/13:00/19:00 JST の全スケジュールで有効）。大きいとOOMのリスク増
# 実行時間短縮のため 8 を推奨（メモリに余裕がある場合）
# TREND_REFRESH_MAX_CONCURRENT=6
# バッチ間の待機秒数（メモリ・接続の解放を待つ）。1 にすると実行時間短縮
# TREND_REFRESH_BATCH_DELAY_SECONDS=2

# 株価取得の最大銘柄数（JP/US 各市場）。45 がデフォルト、60 で従来通り
# STOCK_MAX_TICKERS=45
